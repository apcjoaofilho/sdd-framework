# docs/

Meta-documentation about the SDD Framework repo itself — not the framework's
templates (those live in `templates/`) and not the long-form reference docs (those
live in `references/`).

## Contents

- `IMPLEMENTATION-KICKOFF.md` — self-contained prompt to start implementation in a
  fresh AI session. Relevant until the foundation issues (M3, M1, M6) merge.

## Future

- `history/` — where `PLAN.md` is archived (as `PLAN-v2.md`) after v2.0.0 ships
  (see [PLAN.md §9](../PLAN.md#9-how-this-plan-evolves)).
